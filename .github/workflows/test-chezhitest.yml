name: test chemnet
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -s -c /tmp/cookie.txt "https://china.chemnet.com/fx/intelligence.php"
          curl -s -b /tmp/cookie.txt "https://china.chemnet.com/fx/intelligence.php" | head -c 2000

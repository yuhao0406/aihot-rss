name: test-che

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -s "https://www.icis.com/explore/resources/news/" -H "User-Agent: Mozilla/5.0" | head -c 3000

name: test-che

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -s "https://www.12365auto.com/all/index_1.shtml" | grep -oP '<a[^>]*href="//www.12365auto.com/[^"]*"[^>]*>[^<]+</a>' | head -10
